import React from 'react';
import ReactDOM from 'react-dom/client';
import MistApp from './MistApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MistApp />
  </React.StrictMode>
);